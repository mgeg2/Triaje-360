import { Routes } from '@angular/router';
import { AudioManagerComponent } from 'app/modules/audio-manager/audio-manager.component';

export default [
    {
        path     : '',
        component: AudioManagerComponent,
    },
] as Routes;
